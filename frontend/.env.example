# Frontend Environment Variables

# API Base URL - ใน local development ใช้ proxy ใน package.json
# ใน production ต้องใส่ URL ของ backend
REACT_APP_API_URL=https://assignment-backend-production-9cff.up.railway.app/api

# Admin password สำหรับ teacher login (optional - จะใช้ค่า default ถ้าไม่ใส่)
REACT_APP_ADMIN_PASSWORD=password123